# AetherTasks - Production Environment variables
# Rename this file to '.env' or place it in the frontend directory

# In production, if frontend and backend are hosted on the same origin (e.g. via Nginx or Cloudflare proxy),
# use relative URL path. If they are hosted on separate domains, specify the production API domain.
VITE_API_URL=/api
